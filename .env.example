# ─────────────────────────────────────────────────────────────────────────────
# LLM Providers (at least one required for ingestion and query)
# Priority: Gemini → OpenAI → Minimax → OpenRouter
# ─────────────────────────────────────────────────────────────────────────────

# 1. Google Gemini (primary)
GOOGLE_API_KEY=YOUR_GOOGLE_API_KEY_HERE

# 2. OpenAI (fallback)
OPENAI_API_KEY=YOUR_OPENAI_API_KEY_HERE

# 3. Minimax (fallback)
# GroupId: 19-digit ID from platform.minimax.io → User Center → Basic Information
# See docs/MINIMAX_SETUP.md for setup
MINIMAX_API_KEY=YOUR_MINIMAX_API_KEY_HERE
MINIMAX_GROUP_ID=YOUR_MINIMAX_GROUP_ID_HERE
# MINIMAX_CHAT_MODEL=MiniMax-M2.5  # Optional: MiniMax-M2.5-highspeed, MiniMax-M2.1, MiniMax-M2

# 4. OpenRouter (fallback, free models)
# Get key at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-...
OPENROUTER_CHAT_MODEL=meta-llama/llama-3.2-3b-instruct:free
OPENROUTER_EMBEDDING_MODEL=nvidia/llama-nemotron-embed-vl-1b-v2:free

# ─────────────────────────────────────────────────────────────────────────────
# Other
# ─────────────────────────────────────────────────────────────────────────────

# TranscriptAPI for YouTube video transcripts (required for YouTube URL ingestion)
# https://transcriptapi.com
TRANSCRIPT_API_KEY=YOUR_TRANSCRIPT_API_KEY_HERE

# SQLite and ChromaDB (optional)
DB_PATH=./knowledge_base.db
CHROMA_COLLECTION_NAME=knowledge_base
